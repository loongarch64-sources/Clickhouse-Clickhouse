diff --git a/CMakeLists.txt b/CMakeLists.txt
index 6a824c21c..80b515c14 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -494,6 +494,9 @@ elseif(${CMAKE_SYSTEM_PROCESSOR} MATCHES "^arm*")
 elseif(${CMAKE_SYSTEM_PROCESSOR} STREQUAL "mips")
   # Just to avoid the “unknown processor” error.
   set(ARCH "generic")
+elseif(CMAKE_SYSTEM_PROCESSOR STREQUAL "loongarch64")
+  # Just to avoid the “unknown processor” error.
+  set(ARCH "generic")
 elseif(${CMAKE_SYSTEM_PROCESSOR} STREQUAL "ppc64le")
   set(ARCH "ppc64le")
 else()
diff --git a/include/openssl/base.h b/include/openssl/base.h
index 60c7e870b..dd5435d20 100644
--- a/include/openssl/base.h
+++ b/include/openssl/base.h
@@ -105,6 +105,9 @@ extern "C" {
 #elif defined(__MIPSEL__) && defined(__LP64__)
 #define OPENSSL_64_BIT
 #define OPENSSL_MIPS64
+#elif defined(__loongarch64)
+#define OPENSSL_64_BIT
+#define OPENSSL_LOONGARCH64
 #elif defined(__pnacl__)
 #define OPENSSL_32_BIT
 #define OPENSSL_PNACL
diff --git a/util/generate_build_files.py b/util/generate_build_files.py
index ba87f654e..4b1b9fcaf 100644
--- a/util/generate_build_files.py
+++ b/util/generate_build_files.py
@@ -512,6 +512,9 @@ elseif(${CMAKE_SYSTEM_PROCESSOR} MATCHES "^arm*")
 elseif(${CMAKE_SYSTEM_PROCESSOR} STREQUAL "mips")
   # Just to avoid the “unknown processor” error.
   set(ARCH "generic")
+elseif(CMAKE_SYSTEM_PROCESSOR STREQUAL "loongarch64")
+  # Just to avoid the “unknown processor” error.
+  set(ARCH "generic")
 elseif(${CMAKE_SYSTEM_PROCESSOR} STREQUAL "ppc64le")
   set(ARCH "ppc64le")
 else()
