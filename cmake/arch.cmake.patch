diff --git a/cmake/arch.cmake b/cmake/arch.cmake
index 9604ef62b31..b40cfbfcdbe 100644
--- a/cmake/arch.cmake
+++ b/cmake/arch.cmake
@@ -4,6 +4,9 @@ endif ()
 if (CMAKE_SYSTEM_PROCESSOR MATCHES "^(aarch64.*|AARCH64.*)")
     set (ARCH_AARCH64 1)
 endif ()
+if (CMAKE_SYSTEM_PROCESSOR MATCHES "^(loongarch64.*|LOONGARCH64.*)")
+    set (ARCH_LOONGARCH64 1)
+endif ()
 if (ARCH_AARCH64 OR CMAKE_SYSTEM_PROCESSOR MATCHES "arm")
     set (ARCH_ARM 1)
 endif ()
